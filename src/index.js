/**
 * Block editor shared entry point.
 *
 * Import shared utilities, block filters, or editor styles here.
 * Per-block edit UI lives in blocks/<name>/edit.js — webpack picks those
 * up automatically via webpack.config.js.
 */
